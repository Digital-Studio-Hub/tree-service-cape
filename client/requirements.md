## Packages
framer-motion | Smooth animations and page transitions
react-scroll | Smooth scrolling for anchor links (if single page sections) or internal nav
react-image-lightbox | For the gallery implementation

## Notes
Static images are provided in the assets but I will use Unsplash placeholders for development where specific files aren't mapped yet, or I will assume the file paths match the implementation notes.
The API contract for leads is simple POST /api/leads.
Floating WhatsApp button needs to be global.
Footer needs specific layout with Lekker Network branding.
